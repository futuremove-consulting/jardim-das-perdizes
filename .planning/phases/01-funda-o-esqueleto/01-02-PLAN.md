---
phase: 01-funda-o-esqueleto
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/routes.ts
  - src/lib/routes.test.ts
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/PageLayout.tsx
  - src/app/layout.tsx
  - src/app/globals.css
requirements: [SITE-01]
autonomous: true
must_haves:
  truths:
    - "Every one of the 16 phase-1 routes is reachable from a menu (no dead ends)"
    - "Main nav shows the 6 documented items; secondary nav shows supporting P0 pages"
    - "The URL registry is a single source of truth consumed by nav and later plans"
  artifacts:
    - path: "src/lib/routes.ts"
      provides: "All 16 route paths + pt-BR labels + nav groups"
      exports: ["ROUTES", "NAV_MAIN", "NAV_SECONDARY", "getRouteLabel"]
    - path: "src/components/layout/Header.tsx"
      provides: "Main + secondary nav rendering ROUTES"
    - path: "src/components/layout/Footer.tsx"
      provides: "Institutional footer with CRECI placeholder"
    - path: "src/components/layout/PageLayout.tsx"
      provides: "Shared page shell wrapping Header + main + Footer"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/lib/routes.ts"
      via: "import { NAV_MAIN, NAV_SECONDARY } from '@/lib/routes'"
      pattern: "NAV_MAIN|NAV_SECONDARY"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/PageLayout.tsx"
      via: "imports PageLayout and wraps {children}"
      pattern: "PageLayout"
---

<objective>
Navigation architecture: a single route registry (16 paths + pt-BR labels) enforced by test, plus the design-token base and the Header/Footer/PageLayout shell every page renders.

Purpose: The docs define the menus ("Arquitetura de menus, submenus e páginas.md"); one registry consumed by nav, pages and (later) sitemap prevents URL drift across 16 routes. Output: typed route module with TDD fidelity test, design tokens, and the responsive shell (desktop nav + mobile hierarchical list + institutional footer with CRECI placeholder).

**Phase goal user story:** **As a** visitor interested in Jardim das Perdizes, **I want to** explore hub pages and condominiums with real data and find my path by intent, **so that** I understand the Broker and take the first step toward contact.

Source coverage (this plan): GOAL (arquitetura de navegação dos docs) ✓ · REQ SITE-01 (menu structure for 16 pages) ✓ · RESEARCH (menus doc: 6 main + secondary + mobile simple hierarchical list; footer CRECI) ✓ · CONTEXT (pt-BR labels only in rendered copy, English keys) ✓.
</objective>

<execution_context>
@/home/rochagus/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/rochagus/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-funda-o-esqueleto/SKELETON.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Route registry with fidelity tests (RED → GREEN)</name>
  <files>src/lib/routes.ts, src/lib/routes.test.ts</files>
  <behavior>
    - Test 1: ROUTES contains EXACTLY 16 entries (11 P0 + 5 condominium) — the count is the integrity gate against accidental route additions.
    - Test 2: all 11 P0 paths present: /, /guia-jardim-das-perdizes/, /para-morar/, /para-investir/, /venda-ou-alugue-seu-imovel/, /guias-do-bairro/, /mercado-e-dados/, /encontre-seu-perfil/, /condominios-e-produtos/, /ferramentas/glossario/, /fontes-e-metodo/.
    - Test 3: all 5 condominium paths present under /condominios-e-produtos/: reserva-manaca, bosque-jequitiba, bosque-araucaria, recanto-jacaranda, reserva-figueiras.
    - Test 4: every entry has a non-empty pt-BR label; keys are English slugs (identifier rule — no pt-BR in keys).
    - Test 5: NAV_MAIN has exactly 6 items (Home, Para Morar, Para Investir, Venda ou Alugue seu Imóvel, Guias do Bairro, Mercado & Dados); NAV_SECONDARY contains Condomínios e Produtos, Encontre seu Perfil, Guia do Jardim das Perdizes, Fontes e Método, Glossário — every secondary item's path exists in ROUTES (no dead links).
  </behavior>
  <action>Write src/lib/routes.ts exporting a Route type {path, label}, the ROUTES array with the exact 16 entries and pt-BR labels, NAV_MAIN (6 documented items), NAV_SECONDARY (5 existing P0 pages) and getRouteLabel(path), driving it to green by the tests in the behavior block — write the tests first (RED), then the module (GREEN) per TDD policy.</action>
  <read_first>
    Read extracted/'Sitemap e Blog Map Estratégico.md' section 11 "P0 — fundação e autoridade" for the exact 11 P0 paths; read extracted/'Arquitetura de menus, submenus e páginas.md' section 2 for the 6 main menu items and the secondary list (Sobre a Broker, Fontes e Método, Newsletter, Contato, Pesquisar) — note: Phase 1 has no pages for Sobre/Newsletter/Contato/Pesquisar, so NAV_SECONDARY maps only to routes that EXIST in Phase 1 (SITE-01); do not create dead links. Read SKELETON.md Language rule before writing labels.
  </read_first>
  <acceptance_criteria>routes.test.ts passes: exactly 16 entries; all 16 paths asserted; labels non-empty pt-BR; NAV_MAIN length 6; every NAV_SECONDARY path resolves inside ROUTES. The registry is the only place URLs are declared for phase-1 pages.</acceptance_criteria>
  <done>routes.test.ts green with exactly 16 entries, all 16 paths asserted, non-empty pt-BR labels, NAV_MAIN length 6, all NAV_SECONDARY paths resolving within ROUTES — and these tests were observed failing (RED) before src/lib/routes.ts existed.</done>
  <verify>
    <automated>pnpm test -- routes — all 5 assertions green. pnpm build — clean.</automated>
  </verify>
  <deploy_note>No deploy: registry is build-time data; validated locally via pnpm test + pnpm build.</deploy_note>
</task>

<task type="execute" tdd="true">
  <name>Task 2: Design tokens + Header/Footer/PageLayout shell</name>
  <files>src/app/globals.css, src/components/layout/Header.tsx, src/components/layout/Footer.tsx, src/components/layout/PageLayout.tsx, src/app/layout.tsx</files>
  <behavior>
    - Test (Header smoke): rendering <Header /> shows the 6 main items and the secondary items as links with correct hrefs from NAV_MAIN/NAV_SECONDARY — written before Header exists (RED), then implemented (GREEN).
  </behavior>
  <action>Add the design-token baseline to globals.css via Tailwind v4 CSS-first @theme (brand/ink/paper colors, display/body type scale, spacing rhythm) and a small set of utility classes (.container-page) consistent across pages; implement Header.tsx (brand link to /, desktop main nav + secondary nav row, mobile simple hierarchical list per the menus doc), Footer.tsx (institutional: site name, CRECI placeholder line "CRECI: [inserir número]", contact placeholder text, links to ROUTES secondary pages — no dead links), PageLayout.tsx (Header + <main> + Footer wrapper), and wire PageLayout into src/app/layout.tsx so the identity home from plan 01-01 now renders inside the shell.</action>
  <read_first>
    Read extracted/'Arquitetura de menus, submenus e páginas.md' section 2 (desktop navigation layout), section 3 (mobile: simple hierarchical list; search visible; no fixed "ver imóveis" CTA) and section 8 footer block (footer with privacy, CRECI, contact and accessibility — privacy page is Phase 3, render the label as text without a dead link). Read SKELETON.md Styling row (Tailwind v4 CSS-first tokens). Reuse src/lib/routes.ts from Task 1 — do not redeclare paths.
  </read_first>
  <acceptance_criteria>Header renders exactly the 6 main items with correct hrefs and the 5 secondary items; mobile view collapses to a simple hierarchical list (CSS only, no JS drawer required for the skeleton); Footer shows CRECI placeholder and contact text; root page now renders brand + shell; Header smoke test green; previous tests still green.</acceptance_criteria>
  <done>Header renders the 6 main items + 5 secondary items with correct hrefs (smoke test green); Footer shows CRECI placeholder; root page renders inside the new shell; full suite green; next build clean.</done>
  <verify>
    <automated>pnpm test — all suites green including the Header smoke test. pnpm build — clean.</automated>
  </verify>
  <deploy_note>Local-only: pnpm dev, open http://localhost:3000 and visually check the nav/footer shell; deploy remains Phase 5.</deploy_note>
</task>

</tasks>

## Artifacts this phase produces

- `src/lib/routes.ts` + `src/lib/routes.test.ts` — 16-route registry (English keys, pt-BR labels) + integrity tests
- `src/components/layout/Header.tsx`, `Footer.tsx`, `PageLayout.tsx` — navigation shell
- `src/app/globals.css` — Tailwind v4 @theme design tokens
- `src/app/layout.tsx` — updated to wrap pages in PageLayout

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route registry → nav render | static strings flow into anchor hrefs rendered in DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | package installs | accept | no new packages in this plan; existing toolchain vetted in 01-01 Task 1 |
| T-02-01 | Spoofing | href injection | mitigate | hrefs come only from the typed ROUTES registry (tested), never from user input; no dangerouslySetInnerHTML anywhere |
| T-02-02 | Tampering | XSS via copy | mitigate | all copy rendered as JSX text children; no HTML string interpolation |
</threat_model>

<verification>
pnpm test green (routes + Header smoke); pnpm build clean; running pnpm dev shows the shell with 6 main + 5 secondary menu items on desktop and hierarchical list on mobile viewport.
</verification>

<success_criteria>
The navigation architecture from the docs is live: a tested single-source route registry, design-token base, and a Header/Footer shell every one of the 16 phase-1 pages will share.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-esqueleto/01-02-SUMMARY.md` when done
</output>
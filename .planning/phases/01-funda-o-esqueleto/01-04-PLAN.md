---
phase: 01-funda-o-esqueleto
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - src/app/guia-jardim-das-perdizes/page.tsx
  - src/app/para-morar/page.tsx
  - src/app/para-investir/page.tsx
  - src/app/venda-ou-alugue-seu-imovel/page.tsx
  - src/app/guias-do-bairro/page.tsx
  - src/app/mercado-e-dados/page.tsx
  - src/app/encontre-seu-perfil/page.tsx
  - src/app/ferramentas/glossario/page.tsx
  - src/app/fontes-e-metodo/page.tsx
  - src/app/page.tsx
  - src/lib/seo/schemas.ts
  - src/lib/seo/schemas.test.ts
  - src/app/sitemap.ts
  - src/app/sitemap.test.ts
  - src/app/robots.ts
  - src/app/robots.test.ts
requirements: [SITE-02, SEO-01]
autonomous: true
must_haves:
  truths:
    - "All 16 phase-1 routes exist and every P0 page has title, meta description and canonical"
    - "sitemap.xml lists all 16 URLs; robots.txt allows crawling and points to the sitemap"
    - "Home renders LocalBusiness JSON-LD and 4 intent cards (comprador / locatário / investidor / proprietário) that route to real pages"
    - "Home presents the hub and conversion invitation above the fold in pt-BR (SITE-02 hub part; the dual form+WhatsApp conversion itself is Phase 3)"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "MetadataRoute.Sitemap from ROUTES (16 entries)"
    - path: "src/app/robots.ts"
      provides: "robots.txt allowing all, sitemap URL"
    - path: "src/lib/seo/schemas.ts"
      provides: "localBusinessSchema() JSON-LD object"
      exports: ["localBusinessSchema"]
    - path: "src/app/page.tsx"
      provides: "Hub home with 4 intent cards routing to P0 pages"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/routes.ts"
      via: "import { ROUTES } and map to {url, lastModified, changeFrequency, priority}"
      pattern: "ROUTES"
    - from: "src/app/page.tsx"
      to: "src/lib/routes.ts"
      via: "intent cards link to ROUTES paths (para-morar, para-investir, venda-ou-alugue-seu-imovel)"
      pattern: "NAV_MAIN|ROUTES"
---

<objective>
Full phase-1 route surface + technical SEO + intent-based home: the 9 remaining P0 pages with real pt-BR copy and per-page metadata, sitemap.xml + robots.txt served from the route registry, LocalBusiness JSON-LD on home, and the home rewritten as the hub with 4 intent cards (comprador, locatário, investidor, proprietário) routing to real pages in demo mode.

Purpose: closes SITE-02 (hub + immediate intent routing; the dual conversion CTAs land in Phase 3 CONV-01) and SEO-01's phase-1 slice (sitemap, per-page metadata, canonical, clean URLs, LocalBusiness — RealEstateListing/FAQ schemas are Phases 2/4). Output: a complete, crawlable 16-route site where every route is navigable and every page carries title/description/canonical.

**Phase goal user story:** **As a** visitor interested in Jardim das Perdizes, **I want to** explore hub pages and condominiums with real data and find my path by intent, **so that** I understand the Broker and take the first step toward contact.

Source coverage (this plan): GOAL (SEO técnico base, home roteamento por intenção) ✓ · REQ SITE-02 (hub + roteamento imediato por intenção), SEO-01 (sitemap/metadados/canonical/URLs limpas/LocalBusiness) ✓ · RESEARCH (Home doc: apresentar entidade, proposta editorial e portas de entrada; menu doc CTA fixo "Receba uma análise"; P0 list) ✓ · CONTEXT (pt-BR copy only; demo mocks; Vercel deploy já decidido mas fase 5).
</objective>

<execution_context>
@/home/rochagus/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/rochagus/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-funda-o-esqueleto/SKELETON.md
@.planning/phases/01-funda-o-esqueleto/01-03-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: sitemap + robots + LocalBusiness schema (RED → GREEN)</name>
  <files>src/lib/seo/schemas.ts, src/lib/seo/schemas.test.ts, src/app/sitemap.ts, src/app/sitemap.test.ts, src/app/robots.ts, src/app/robots.test.ts</files>
  <behavior>
    - Test 1 (sitemap): sitemap() returns exactly 16 entries; every entry url starts with siteUrl() from src/lib/config.ts and is an absolute URL; paths match ROUTES.
    - Test 2 (robots): robots() returns rules allowing all user agents and a sitemap entry pointing to siteUrl() + /sitemap.xml.
    - Test 3 (schema): localBusinessSchema() returns an object with @type "RealEstateAgent" (or LocalBusiness), a pt-BR name "Jardim das Perdizes Broker", address in São Paulo — valid JSON-serializable structure.
  </behavior>
  <action>Write the three test files first (RED — modules missing), then implement src/lib/seo/schemas.ts (typed JSON-LD object builder per the behavior; serialized with JSON.stringify at render time — no string interpolation into the script), src/app/sitemap.ts (MetadataRoute.Sitemap mapping ROUTES to {url: siteUrl()+path, lastModified set to the phase delivery date, changeFrequency "monthly", priority tiered: home 1.0, P0 0.8, condominium 0.7}), and src/app/robots.ts per Next.js rules — making all tests green.</action>
  <read_first>
    Read .planning/REQUIREMENTS.md SEO-01 (sitemap.xml, canonical, URLs limpas; RealEstateListing/FAQ deferred to Phases 2/4 — only LocalBusiness here) and ROADMAP success criterion 3 (sitemap.xml + robots.txt served; LocalBusiness on home). Reuse siteUrl() from src/lib/config.ts and ROUTES from src/lib/routes.ts — do not redeclare paths or URL logic.
  </read_first>
  <acceptance_criteria>sitemap() yields 16 absolute URLs derived from ROUTES; robots() allows all + sitemap link; localBusinessSchema() is a valid JSON-LD object; all three suites green; pnpm build clean.</acceptance_criteria>
  <done>sitemap() yields 16 absolute URLs from ROUTES; robots() allows all + sitemap link; localBusinessSchema() is valid JSON-LD; RED observed first; /sitemap.xml and /robots.txt serve 200 under pnpm dev; build clean.</done>
  <verify>
    <automated>pnpm test -- sitemap robots schemas — all green. pnpm build — clean. pnpm dev: curl /sitemap.xml (200, contains /condominios-e-produtos/) and /robots.txt (200, contains Sitemap:).</automated>
  </verify>
  <deploy_note>Local-only: /sitemap.xml and /robots.txt are served by the dev server; same behavior on Vercel in Phase 5.</deploy_note>
</task>

<task type="execute">
  <name>Task 2: The 9 remaining P0 pages with real copy + per-page metadata</name>
  <files>src/app/guia-jardim-das-perdizes/page.tsx, src/app/para-morar/page.tsx, src/app/para-investir/page.tsx, src/app/venda-ou-alugue-seu-imovel/page.tsx, src/app/guias-do-bairro/page.tsx, src/app/mercado-e-dados/page.tsx, src/app/encontre-seu-perfil/page.tsx, src/app/ferramentas/glossario/page.tsx, src/app/fontes-e-metodo/page.tsx</files>
  <action>Create the 9 P0 page.tsx files (guia-jardim-das-perdizes, para-morar, para-investir, venda-ou-alugue-seu-imovel, guias-do-bairro, mercado-e-dados, encontre-seu-perfil, ferramentas/glossario, fontes-e-metodo) inside PageLayout, each with a real pt-BR copy block derived from the research docs (2-4 sentences of genuine editorial content per page — no lorem, no invented prices), an h1 with the route label, and export const metadata = buildPageMetadata({title, description, path}) per the factory from plan 01-03, so every P0 page carries title, description and canonical (ROADMAP criterion 2).</action>
  <read_first>
    Read extracted/'Arquitetura de menus, submenus e páginas.md' sections 3.1–3.7 for the editorial purpose of each P0 section (Para Morar = stages and decision modes; Para Investir = renda/locação + estratégia de revenda, no promises; Venda ou Alugue = "seu imóvel" captive proposition; Guias do Bairro = awareness engine; Mercado & Dados = research room; Fontes e Método = governance, nivel de evidência). Copy must be honest and sourced (state what IS verified); never invent prices/towers in copy. Titles use ROUTES labels; schema in English, copy pt-BR.
  </read_first>
  <acceptance_criteria>9 routes render with 200 and h1 labels; each page.tsx exports metadata from the factory (grep: buildPageMetadata in all 9 + the 5 condominium + home); pt-BR copy present, no placeholder lorem, no invented data points; all 16 phase-1 routes now exist and are navigable from menus (SITE-01 complete).</acceptance_criteria>
  <done>9 routes render 200 with h1 labels; grep confirms buildPageMetadata exported in all 15 phase-1 page files; no lorem placeholder text; no invented data points; build clean.</done>
  <verify>
    <automated>grep -rl "buildPageMetadata" src/app --include="*.tsx" | wc -l returns 15 page files... use: for p in guia-jardim-das-perdizes para-morar para-investir venda-ou-alugue-seu-imovel guias-do-bairro mercado-e-dados encontre-seu-perfil ferramentas/glossario fontes-e-metodo; do test -f "src/app/$p/page.tsx" || exit 1; done — then pnpm build (clean) and pnpm dev 200s on 3 sampled routes.</automated>
  </verify>
  <deploy_note>Local-only via pnpm dev/build; Vercel deploy Phase 5.</deploy_note>
</task>

<task type="execute">
  <name>Task 3: Home as hub with LocalBusiness JSON-LD + intent routing (demo)</name>
  <files>src/app/page.tsx, src/app/page.test.tsx</files>
  <action>Rewrite src/app/page.tsx as the hub home (SITE-02): above the fold present the Broker entity + editorial proposal and the four intent cards (Comprador → /para-morar, Locatário → /para-investir/para-locacao is NOT P0 — use /guias-do-bairro? NO: use existing phase-1 routes only: Comprador → /para-morar, Locatário → /encontre-seu-perfil, Investidor → /para-investir, Proprietário → /venda-ou-alugue-seu-imovel) with clear arrow CTAs, embed the LocalBusiness JSON-LD script from src/lib/seo/schemas.ts (JSON.stringify of localBusinessSchema()), add a demo-mode banner driven by isDemo() from src/lib/config.ts, and extend src/app/page.test.tsx with assertions that the 4 intent links render with correct hrefs and the JSON-LD script tag exists (tests first per TDD policy), keeping the existing brand smoke assertions green.</action>
  <read_first>
    Read extracted/'Arquitetura de menus, submenus e páginas.md' section 1.1 Home (present entity, editorial proposal, user entry doors; NOT a listings push in the first viewport) and section 2 (fixed CTA should be "Receba uma análise"/"Fale com um especialista local" — the actual form/WhatsApp conversion is Phase 3 CONV-01, so Phase 1 renders the intent cards + a text CTA line without a broken form). Read SKELETON.md Out of Scope (dual conversion deferred). Hrefs MUST point only to routes existing in Phase 1 (ROUTES). Data fidelity: no tower/price claims on home.
  </read_first>
  <acceptance_criteria>Home renders above the fold: entity + proposal + 4 intent cards linking to real phase-1 routes + demo banner (visible only when isDemo()); JSON-LD script tag with @type RealEstateAgent present in HTML; intent-card href assertions and JSON-LD assertion in page.test.tsx green; all previous suites green; next build clean.</acceptance_criteria>
  <done>Home suite green (4 intent hrefs + ld+json script tag assertions); curl / returns 1 ld+json script and the para-morar link; demo banner only when isDemo(); build clean.</done>
  <verify>
    <automated>pnpm test — home suite green (4 hrefs + JSON-LD tag). pnpm build — clean. pnpm dev: curl -s http://localhost:3000 | grep -c "ld+json" returns 1 and grep "para-morar" present.</automated>
  </verify>
  <deploy_note>Home behaves identically under pnpm start; Vercel deploy Phase 5.</deploy_note>
</task>

</tasks>

## Artifacts this phase produces

- `src/lib/seo/schemas.ts` + `schemas.test.ts` — LocalBusiness JSON-LD builder
- `src/app/sitemap.ts` + `sitemap.test.ts` — 16-entry sitemap from ROUTES
- `src/app/robots.ts` + `robots.test.ts` — robots.txt
- 9 P0 page files (`src/app/{...}/page.tsx`) with real pt-BR copy + factory metadata
- `src/app/page.tsx` + `page.test.tsx` — hub home with intent routing + JSON-LD + demo banner

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env siteUrl → sitemap/robots | NEXT_PUBLIC_SITE_URL value flows into crawled XML output |
| JSON-LD object → <script> | serialized schema crosses into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | package installs | accept | no new packages in this plan; toolchain vetted in 01-01 |
| T-04-01 | Tampering | JSON-LD injection | mitigate | schema built by typed builder and emitted via JSON.stringify — no string interpolation, tested (schemas.test.ts) |
| T-04-02 | Spoofing | canonical/sitemap URL | mitigate | URLs come solely from siteUrl() (env with fallback) + ROUTES; test asserts absolute-url invariant |
| T-04-03 | Information Disclosure | demo banner in prod | mitigate | banner gated by isDemo() from config; prod build renders no demo markers (ROADMAP criterion 4) |
</threat_model>

<verification>
All 16 routes exist and return 200; pnpm test green across suites (config, routes, condominiums, metadata, sitemap/robots/schemas, home); pnpm build clean; /sitemap.xml and /robots.txt served; home contains LocalBusiness JSON-LD and 4 intent links.
</verification>

<success_criteria>
Phase-1 success criteria all met: (1) navigation across home/para-morar/para-investir/condominios-e-produtos without route breakage; (2) every P0 page has title/description/canonical; (3) sitemap.xml + robots.txt served and LocalBusiness on home; (4) APP_MODE=demo renders sample content and prod build is clean (mocks isolated); (5) pnpm project runs, next build clean, TDD tests pass.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-esqueleto/01-04-SUMMARY.md` when done
</output>
---
phase: 01-funda-o-esqueleto
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/data/condominiums.ts
  - src/data/condominiums.test.ts
  - src/lib/seo/metadata.ts
  - src/lib/seo/metadata.test.ts
  - src/app/condominios-e-produtos/page.tsx
  - src/app/condominios-e-produtos/[slug]/page.tsx
requirements: [SITE-01, SITE-05]
autonomous: true
must_haves:
  truths:
    - "A visitor opens /condominios-e-produtos and sees the 5 condominium cards"
    - "Each card links to a condominium page that renders real, confirmed product data (towers, units, area, delivery status)"
    - "No invented data: confirmed tower names appear exactly; Recanto Jacarandá shows towers as 'not publicly identified'"
    - "Each condominium page emits title, meta description and canonical"
  artifacts:
    - path: "src/data/condominiums.ts"
      provides: "Typed Condominium records for the 5 phase-1 condominiums"
      exports: ["CONDOMINIUMS", "getCondominiumBySlug"]
    - path: "src/app/condominios-e-produtos/[slug]/page.tsx"
      provides: "Dynamic condominium page with generateStaticParams and per-page metadata"
    - path: "src/lib/seo/metadata.ts"
      provides: "Metadata factory (title, description, canonical from NEXT_PUBLIC_SITE_URL)"
      exports: ["buildPageMetadata"]
  key_links:
    - from: "src/app/condominios-e-produtos/[slug]/page.tsx"
      to: "src/data/condominiums.ts"
      via: "import { getCondominiumBySlug } and .notFound() on miss"
      pattern: "getCondominiumBySlug|notFound"
    - from: "src/app/condominios-e-produtos/[slug]/page.tsx"
      to: "src/lib/seo/metadata.ts"
      via: "export const metadata = buildPageMetadata(...)"
      pattern: "buildPageMetadata"
---

<objective>
Condominium data layer + discovery of condo pages: a fidelity-tested typed module holding the 5 phase-1 condominiums, a dynamic route rendering each real product, and a metadata factory (title/description/canonical) that every page reuses.

Purpose: SITE-05 (estrutura das páginas de condomínio) with the data-fidelity rule — confirmed tower names render exactly, unconfirmed names render "não identificada publicamente". Output: clickable condominium pages with real data and per-page SEO metadata, plus the reusable metadata factory consumed by the 9 remaining P0 pages in plan 01-04.

**Phase goal user story:** **As a** visitor interested in Jardim das Perdizes, **I want to** explore hub pages and condominiums with real data and find my path by intent, **so that** I understand the Broker and take the first step toward contact.

Source coverage (this plan): GOAL (5 páginas de condomínio, SEO base) ✓ · REQ SITE-01 (condominium routes), SITE-05 (produto, torres confirmadas, status de entrega) ✓ · RESEARCH (Dados consolidados + Matriz completa de torres: all 5 condominium facts below) ✓ · CONTEXT (data fidelity: nunca inventar) ✓.
</objective>

<execution_context>
@/home/rochagus/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/rochagus/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-funda-o-esqueleto/SKELETON.md
@.planning/phases/01-funda-o-esqueleto/01-02-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Condominium data module with fidelity tests (RED → GREEN)</name>
  <files>src/data/condominiums.ts, src/data/condominiums.test.ts</files>
  <behavior>
    - Test 1: CONDOMINIUMS has exactly 5 entries with unique slugs (reserva-manaca, bosque-jequitiba, bosque-araucaria, recanto-jacaranda, reserva-figueiras).
    - Test 2 (fidelity, Manacá): towers array is exactly [{tower:"A", blocks:["Andorinha","Sabiá"]},{tower:"B", blocks:["Arara","Cacatua"]},{tower:"C", blocks:["Falcão","Jacutinga"]}]; units=320; areaMin=241; areaMax=283; deliveryStatus="delivered"; deliveryDate "2016-04".
    - Test 3 (fidelity, Jequitibá): towers exactly Canário/Bem-te-vi/Beija-flor; units=320; areaMin=159; areaMax=331; first delivery 2015-10; parking="3–4 vagas".
    - Test 4 (fidelity, Araucária): towers exactly Tiê/Tucano; units=212; areaMin=157; areaMax=170; delivery 2016-02.
    - Test 5 (fidelity, Jacarandá): towers array EMPTY with towersNote="Torres não identificadas publicamente"; units=396; areaMin=79; areaMax=161; parking="2 vagas"; delivery 2016-07; monthlyFee "R$ 1.450 (condomínio médio informado)".
    - Test 6 (fidelity, Figueiras): towers exactly [{tower:"A", blocks:["Subcondomínio Torre A"]}] or towersNote only; units=104; deliveryStatus="under-construction"; deliveryDate "2026-10 (previsão)"; launch "2023-02".
    - Test 7: getCondominiumBySlug returns the record or undefined for unknown slugs.
  </behavior>
  <action>Write the fidelity tests first (RED — module missing), then implement src/data/condominiums.ts: a Condominium type {slug, name, towers?: {tower, blocks}[], towersNote?, units, areaMin, areaMax, parking?, deliveryStatus: "delivered"|"under-construction", deliveryDate, launch?, monthlyFee?, blurb} plus CONDOMINIUMS (5 records with EXACTLY the confirmed facts above and pt-BR copy only in display fields) and getCondominiumBySlug, making the tests green — every number and tower name must match the research docs, nothing invented.</action>
  <read_first>
    Read extracted/'Dados consolidados de torres e produtos — fonte Viva Jardim das Perdizes.md' (fiche of the 4 first-cycle condominiums; nominal tower names from Plataforma de Vendas; figueiras from apto.vc/Tecnisa) and extracted/'Matriz completa de torres e produtos.md' rows for the 5 condominiums. Data-fidelity rule: Recanto Jacarandá tower names are NOT confirmed anywhere — the module MUST NOT invent them; use towersNote text. Reserva Figueiras tower = "Subcondomínio Torre A" (Tecnisa) with under-construction status.
  </read_first>
  <acceptance_criteria>All 7 fidelity tests green; every tower name, unit count and area in the module verbatim-matches the research docs (asserted); unknown slug returns undefined; pnpm build still clean.</acceptance_criteria>
  <done>All 7 fidelity tests green; every tower name, unit count and area verbatim-matches the research docs (asserted in tests); RED state observed before the module existed; build clean.</done>
  <verify>
    <automated>pnpm test -- condominiums — 7 suites green. pnpm build — clean.</automated>
  </verify>
  <deploy_note>No deploy: data module is build-time typed data, validated via tests and build.</deploy_note>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Metadata factory with canonical tests (RED → GREEN)</name>
  <files>src/lib/seo/metadata.ts, src/lib/seo/metadata.test.ts</files>
  <behavior>
    - Test 1: buildPageMetadata({title, description, path}) returns {title, description, alternates: {canonical}} with canonical = NEXT_PUBLIC_SITE_URL + path (trailing-slash-safe join).
    - Test 2: with NEXT_PUBLIC_SITE_URL set to http://localhost:3000 and path /condominios-e-produtos/reserva-manaca/, canonical is exactly http://localhost:3000/condominios-e-produtos/reserva-manaca/.
    - Test 3: when the env var is unset, the factory falls back to a safe default (production domain https://www.jardimdasperdizes.com.br) and still produces a valid absolute canonical.
  </behavior>
  <action>Write metadata.test.ts first (RED), then implement src/lib/seo/metadata.ts exporting buildPageMetadata({title, description, path}) that returns Next.js Metadata with title, description and alternates.canonical assembled from siteUrl() in src/lib/config.ts (plan 01-01) — no duplicate URL logic, no interpolation of user input; single canonical per page per SEO-01.</action>
  <read_first>
    Read .planning/REQUIREMENTS.md SEO-01 (metadados por página + canonical; schema.org full scope is split — LocalBusiness in plan 01-04, RealEstateListing/FAQ in later phases) and ROADMAP phase 1 success criterion 2 (every P0 page has title, meta description, canonical; schema in English, pt-BR in copy). Reuse siteUrl() from src/lib/config.ts — do not re-read process.env here.
  </read_first>
  <acceptance_criteria>All 3 metadata tests green; canonical always absolute and env-driven; no duplicate URL-building code; previous suites (routes, config, condominiums) still pass.</acceptance_criteria>
  <done>All 3 metadata tests green with absolute canonical assembled from siteUrl(); RED observed first; no duplicate URL logic; full suite green.</done>
  <verify>
    <automated>pnpm test -- metadata — 3 suites green. pnpm build — clean.</automated>
  </verify>
  <deploy_note>No deploy: metadata factory is build-time; validated via tests.</deploy_note>
</task>

<task type="execute">
  <name>Task 3: Condominium index + dynamic [slug] page rendering real data</name>
  <files>src/app/condominios-e-produtos/page.tsx, src/app/condominios-e-produtos/[slug]/page.tsx, src/app/condominios-e-produtos/[slug]/page.test.tsx</files>
  <action>Implement the condominiums index page (src/app/condominios-e-produtos/page.tsx) rendering 5 cards from CONDOMINIUMS (name, blurb, units/area summary, status badge delivered vs em construção, link to each slug — metadata via buildPageMetadata) and the dynamic route (src/app/condominios-e-produtos/[slug]/page.tsx) with generateStaticParams from CONDOMINIUMS, notFound() for unknown slugs, and sections: product summary, towers table (tower names rendered as confirmed or towersNote text), units/area/parking/delivery facts, pt-BR copy only; add a render smoke test (renders Manacá page content and the Jacarandá "não identificadas" text) written with the page per TDD policy.</action>
  <read_first>
    Read extracted/'Cards e fichas técnicas de imóveis, tipologias e condomínios.md' card/ficha section expectations (product, towers, status de entrega per SITE-05) and SKELETON.md Data layer row (demo-first typed module; Supabase in Phase 2). Reuse PageLayout from plan 01-02 (pages render inside the shell). Use only facts present in src/data/condominiums.ts — no invented towers/units/prices.
  </read_first>
  <acceptance_criteria>GET /condominios-e-produtos returns 5 cards linking to slugs; GET /condominios-e-produtos/reserva-manaca renders towers Andorinha/Sabiá/Arara/Cacatua/Falcão/Jacutinga and delivery status; /condominios-e-produtos/recanto-jacaranda shows the towersNote "não identificadas publicamente" text; unknown slug returns 404 via notFound(); every page has title, description and canonical from the factory; smoke test green.</acceptance_criteria>
  <done>curl 200 on the index and on a delivered condominium page (reserva-manaca) and on the unknown-slug 404 path; smoke test green; build clean.</done>
  <verify>
    <automated>pnpm test — full suite green including the new page smoke test. pnpm build — clean (dynamic routes prerendered via generateStaticParams). pnpm dev — curl -o /dev/null -w "%{http_code}" on /condominios-e-produtos (200) and /condominios-e-produtos/reserva-manaca (200).</automated>
  </verify>
  <deploy_note>Local-only verification via pnpm dev/build; Vercel deploy stays Phase 5.</deploy_note>
</task>

</tasks>

## Artifacts this phase produces

- `src/data/condominiums.ts` + `condominiums.test.ts` — fidelity-tested data module (5 condominiums)
- `src/lib/seo/metadata.ts` + `metadata.test.ts` — metadata factory with canonical support
- `src/app/condominios-e-produtos/page.tsx` — condominium index (5 cards)
- `src/app/condominios-e-produtos/[slug]/page.tsx` + `page.test.tsx` — dynamic condominium page + smoke test

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data module → DOM | typed condominium records render as page content |
| URL slug → route params | user-supplied slug crosses into generateStaticParams / notFound logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | package installs | accept | no new packages in this plan; toolchain vetted in 01-01 |
| T-03-01 | Tampering | data fidelity | mitigate | fidelity tests assert exact tower names/units/areas; Jacarandá towers render only as towersNote text — invented data is a test failure |
| T-03-02 | DoS/Spoofing | unknown slug | mitigate | generateStaticParams only whitelists the 5 slugs; unknown paths hit notFound() → 404, never a fabricated page |
| T-03-03 | Tampering | XSS via copy | mitigate | all records are static build-time strings rendered as JSX text; no dangerouslySetInnerHTML |
</threat_model>

<verification>
pnpm test green (7 fidelity + 3 metadata + smoke); pnpm build clean; pnpm dev serves index and both a delivered (Manacá) and an under-construction (Figueiras) condominium page with 200; Jacarandá page shows the unconfirmed-towers disclosure.
</verification>

<success_criteria>
A visitor can browse the 5 condominium pages with real confirmed data (tower names, units, areas, delivery status) and never sees invented facts; each page carries title, description and canonical via the shared factory.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-esqueleto/01-03-SUMMARY.md` when done
</output>
---
phase: 01-funda-o-esqueleto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - vitest.config.ts
  - src/test/setup.ts
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/app/page.test.tsx
  - src/lib/config.ts
  - src/lib/config.test.ts
  - .env.local
  - .env.local.example
  - AGENTS.md
autonomous: false
requirements: [SITE-01]
must_haves:
  truths:
    - "pnpm dev boots the app and the root page renders the Broker identity"
    - "APP_MODE=demo is the default; switching to prod does not break the build"
    - "pnpm test runs TDD tests (RED before implementation per task)"
  artifacts:
    - path: "src/lib/config.ts"
      provides: "APP_MODE reader (demo|prod) with safe defaults"
      exports: ["appMode", "isDemo", "isProd", "siteUrl"]
    - path: "src/app/page.tsx"
      provides: "Identity root page with pt-BR copy"
      contains: "Jardim das Perdizes"
    - path: "src/app/page.test.tsx"
      provides: "Smoke test asserting root page brand content"
    - path: "vitest.config.ts"
      provides: "Vitest + jsdom + testing-library wiring"
  key_links:
    - from: "src/lib/config.ts"
      to: "src/lib/config.test.ts"
      via: "import { appMode } from './config'"
      pattern: "appMode|isDemo"
    - from: "src/app/page.tsx"
      to: "src/app/page.test.tsx"
      via: "render(<Home />) asserting heading text"
      pattern: "getByRole|getByText"
---

<objective>
Walking skeleton for the site: the thinnest end-to-end stack that proves scaffold runs, builds, tests and renders the Broker identity.

**Phase goal user story:** **As a** visitor interested in Jardim das Perdizes, **I want to** explore hub pages and condominiums with real data and find my path by intent, **so that** I understand the Broker and take the first step toward contact. (NOTE: ROADMAP.md Phase 1 Goal line is not in the user-story template form — the story above is derived from phase description + SITE-02. Surface to user for confirmation.)

Purpose: Establish every architectural decision later slices build on, locked in SKELETON.md (framework, styling, data-layer strategy, test runner, pnpm, language rule). Output: a running Next.js App Router project with APP_MODE demo-first config, identity home page, Vitest TDD wiring and a documented full-stack run command.

Source coverage (this plan): GOAL (site de pé, toggle demo, build/test green) ✓ · REQ SITE-01 (base scaffold for 16 pages) ✓ · RESEARCH (build-first stack Next+TS+pnpm; demo-first APP_MODE) ✓ · CONTEXT (English identifiers, pt-BR copy, pnpm only) ✓. Full phase audit covers all 16 routes: delivered in plans 01-02/01-03 and 9 P0 pages in 01-04 — no unplanned source items.
</objective>

<execution_context>
@/home/rochagus/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/rochagus/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify package legitimacy before install</name>
  <files>No source files — pre-flight gate on packages to install: next, react, react-dom, typescript, tailwindcss, vitest, @testing-library/react, @testing-library/jest-dom, jsdom, eslint-config-next</files>
  <action>Confirm on npmjs.com that each listed package is published by its official publisher (Vercel for next, Meta for react, tailwindlabs, vitest-dev, testing-library org, jsdom maintainers) and reply approved or list any suspicious package; RESEARCH.md has no Package Legitimacy Audit table so the fallback policy marks these [ASSUMED] and requires this blocking checkpoint before any package-manager install.</action>
  <verify>
    <automated>Human-gated: approval text required; see how-to-verify steps.</automated>
  </verify>
  <done>All packages vetted as official; user replied approved; proceed to Task 2 install.</done>
  <what-built>No code — a pre-flight legitimacy gate for the exact packages plan 01-01 will install (see files list).</what-built>
  <how-to-verify>
    1. Open npmjs.com/package/next and confirm the package is the official Vercel Next.js (high downloads, corporate publisher).
    2. Open npmjs.com/package/vitest, /@testing-library/react, /jsdom, /tailwindcss and confirm official publishers (vitest-dev, testing-library, jsdom maintainers, tailwindlabs).
    3. Reply with the word approved or list any package that looks wrong/malicious.
  </how-to-verify>
  <resume-signal>approved (or describe a rejected package)</resume-signal>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Scaffold Next.js App Router + TDD wiring (RED on app-shell smoke)</name>
  <files>package.json, next.config.ts, tsconfig.json, vitest.config.ts, src/test/setup.ts, src/app/layout.tsx, src/app/globals.css, src/app/page.test.tsx</files>
  <behavior>
    - Test 1 (smoke): rendering src/app/page.tsx produces a heading containing the site brand — written BEFORE the identity page exists, so it must FAIL (RED).
    - Test 2 (config RED): src/lib/config.test.ts asserting appMode() defaults to 'demo' when APP_MODE is unset and to the provided value when set.
  </behavior>
  <action>Run create-next-app in-place with TypeScript, App Router, Tailwind, ESLint and src dir enabled (pnpm create next-app . --ts --tailwind --eslint --app --src-dir --use-pnpm), add vitest + @testing-library/react + jsdom + @testing-library/jest-dom as devDependencies, write vitest.config.ts and src/test/setup.ts, add a test:script to package.json, and write the RED tests (app-shell smoke + config) per the behavior block before any implementation code per TDD policy.</action>
  <read_first>
    Read .planning/ROADMAP.md phase 1 success criteria 1 and 5 (pnpm dev navigation, clean next build, TDD tests passing); read SKELETON.md Architectural Decisions rows Framework/Styling/Test runner/Package manager to lock the versions and structure conventions (Next.js 15 App Router, Tailwind v4 CSS-first, Vitest+jsdom). If create-next-app refuses the non-empty directory (docs/, extracted/, .planning/ exist), scaffold in a temp dir and move the generated files into the repo root, keeping .planning/, docs/, extracted/ untouched.
  </read_first>
  <acceptance_criteria>create-next-app completed with src/ dir; vitest.config.ts uses the jsdom environment for files matching **/*.test.{ts,tsx}; src/test/setup.ts imports @testing-library/jest-dom; package.json test script runs vitest run; the smoke test src/app/page.test.tsx fails in RED state (asserts a brand heading that src/app/page.tsx does not yet render); the config test failure demonstrates the missing src/lib/config.ts module.</acceptance_criteria>
  <done>RED state proven: pnpm test reports failures for the two new test files (module not found / assertion), proving test wiring works and the target code is absent.</done>
  <verify>
    <automated>pnpm test — expected: the two RED test files fail with module-not-found/assertion errors proving the tests are wired and the target code is absent. pnpm build — expected: succeeds even with pending home content (default template page compiles).</automated>
  </verify>
  <deploy_note>No deploy in this task: pnpm dev boots the local shell at http://localhost:3000 (default Next template page); Vercel deploy is Phase 5.</deploy_note>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: GREEN — APP_MODE config + identity home + walking-skeleton run documentation</name>
  <files>src/lib/config.ts, src/lib/config.test.ts, src/app/page.tsx, src/app/layout.tsx, .env.local, .env.local.example, AGENTS.md, package.json</files>
  <behavior>
    - Test 1: appMode() returns 'demo' when process.env.APP_MODE is undefined ('demo' fallback), returns 'prod' when APP_MODE=prod, and throws or falls back for invalid values (keep strict: only 'demo'|'prod' accepted, default 'demo').
    - Test 2: isDemo() is true in demo; isProd() is true in prod.
    - Test 3: siteUrl() reads NEXT_PUBLIC_SITE_URL with a safe fallback (default https://www.jardimdasperdizes.com.br for prod, http://localhost:3000 in dev/demo).
  </behavior>
  <action>Implement src/lib/config.ts (appMode, isDemo, isProd, siteUrl reading process.env with the fallback rules above), rewrite src/app/page.tsx as the identity home (pt-BR copy: brand "Jardim das Perdizes Broker", one-line positioning as independent hyperlocal hub, section pointing to "Condomínios e Produtos" and "Para Morar"), set lang="pt-BR" and the pt-BR metadata on src/app/layout.tsx, create .env.local with APP_MODE=demo and NEXT_PUBLIC_SITE_URL=http://localhost:3000 plus .env.local.example documenting both vars, write AGENTS.md capturing project rules (pnpm only, English identifiers, pt-BR copy, TDD policy, APP_MODE toggle), and verify the full local stack (pnpm dev curls 200 on /, pnpm build clean, pnpm test green) per the walking-skeleton deployment checklist.</action>
  <read_first>
    Read SKELETON.md "Stack Touched in Phase 1" and "Architectural Decisions" (Language rule: pt-BR copy only in rendered text; identifiers in English); read the extracted doc 'Arquitetura de menus, submenus e páginas.md' section 1.1 Home (home must present the entity, editorial proposal and user entry doors — not listings). Copy must state positioning; DO NOT invent towers/prices on home (data fidelity rule).
  </read_first>
  <acceptance_criteria>All three config tests pass and the smoke test passes (root page renders "Jardim das Perdizes" heading); APP_MODE=prod exercises the same build without runtime breakage (mocks isolated behind config, no demo imports outside data layer); pnpm dev serves / with HTTP 200; next build completes with zero errors; AGENTS.md exists; SKELETON.md checklist for Phase 1 matches the delivered stack.</acceptance_criteria>
  <done>Green state proven: pnpm test all green (config + smoke); pnpm build clean; curl / returns 200 under pnpm dev; APP_MODE=prod build also clean; AGENTS.md, .env.local, .env.local.example exist.</done>
  <verify>
    <automated>pnpm test — all tests green. pnpm build — clean, no errors. (cd "$(git rev-parse --show-toplevel)" 2>/dev/null || pwd) with pnpm dev in background — curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 returns 200. APP_MODE=prod pnpm build — still clean.</automated>
  </verify>
  <deploy_note>Documented dev run: pnpm dev (http://localhost:3000). Documented prod-mode smoke: pnpm build && pnpm start. Actual Vercel deployment remains Phase 5 (per ROADMAP).</deploy_note>
</task>

</tasks>

## Artifacts this phase produces

- `package.json`, `next.config.ts`, `tsconfig.json`, `postcss.config.mjs`, `eslint.config.mjs`, `vitest.config.ts`, `src/test/setup.ts` — build/lint/test toolchain
- `src/app/layout.tsx`, `src/app/globals.css` — root layout + Tailwind v4 entry
- `src/app/page.tsx` + `src/app/page.test.tsx` — identity home + smoke test
- `src/lib/config.ts` + `src/lib/config.test.ts` — APP_MODE demo-first config + TDD tests
- `.env.local` + `.env.local.example` — APP_MODE and NEXT_PUBLIC_SITE_URL
- `AGENTS.md` — project conventions
- `SKELETON.md` (same dir) — walking skeleton contract

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → runtime config | process.env values (APP_MODE, NEXT_PUBLIC_SITE_URL) cross into server/client bundles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | blocking-human checkpoint (Task 1) vetting next/react/vitest/testing-library on npmjs.com before create-next-app install; all [ASSUMED] per fallback policy |
| T-01-01 | Information Disclosure | NEXT_PUBLIC_* env vars | mitigate | only NEXT_PUBLIC_SITE_URL/APP_MODE reach client bundles; secrets never prefix NEXT_PUBLIC_ — documented in AGENTS.md |
| T-01-02 | Tampering | build output | accept | static content site, pinned toolchain, no third-party runtime scripts yet |
</threat_model>

<verification>
pnpm test green (config + smoke suites); pnpm build zero errors; APP_MODE=prod build unchanged; pnpm dev serves / with 200; AGENTS.md and .env.local.example present.
</verification>

<success_criteria>
The walking skeleton runs end-to-end locally: dev server serves the identity page, all TDD tests pass (RED was observed in Task 2), prod-mode build is clean, and SKELETON.md records the decisions later phases build on without renegotiation.
</success_criteria>

<output>
Create `.planning/phases/01-funda-o-esqueleto/01-01-SUMMARY.md` when done
</output>